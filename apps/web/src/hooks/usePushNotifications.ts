import { useState } from "react";
import { apiRequest } from "../lib/api";
export function usePushNotifications() { const [status, setStatus] = useState<NotificationPermission | "unsupported">(typeof Notification === "undefined" ? "unsupported" : Notification.permission); const enable = async () => { if (typeof Notification === "undefined" || !navigator.serviceWorker) return setStatus("unsupported"); const permission = await Notification.requestPermission(); setStatus(permission); if (permission !== "granted") return; const registration = await navigator.serviceWorker.ready; const subscription = await registration.pushManager.subscribe({ userVisibleOnly: true, applicationServerKey: import.meta.env.VITE_PUSH_VAPID_PUBLIC_KEY }); await apiRequest("/v1/notifications/register", { method: "POST", body: JSON.stringify({ subscription: subscription.toJSON() }) }); }; return { status, enable }; }
