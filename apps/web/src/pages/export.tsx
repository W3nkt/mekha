export function ExportPage() { const download = (type: string) => { const sellerId = window.prompt("Seller ID"); if (!sellerId) return; window.location.href = `https://mekha-api.wen-kt2020.workers.dev/v1/sellers/${sellerId}/export?type=${type}`; }; return <section className="page-enter export-page"><h1>ດາວໂຫລດຂໍ້ມູນ</h1><p>ສົ່ງອອກຂໍ້ມູນສຳລັບ MOIC ແລະ ການເກັບບັນທຶກ.</p>{[["orders", "ຄຳສັ່ງທັງໝົດ"], ["monthly", "ສະຫຼຸບລາຍເດືອນ"], ["customers", "ລາຍຊື່ລູກຄ້າ"]].map(([type, label]) => <button className="primary-link" type="button" onClick={() => download(type)} key={type}>ດາວໂຫລດ {label} CSV</button>)}</section>; }
