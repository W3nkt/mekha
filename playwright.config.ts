import { defineConfig, devices } from "@playwright/test";
export default defineConfig({ testDir: "./e2e", fullyParallel: false, retries: process.env.CI ? 2 : 0, reporter: [["html"], ["line"]], use: { baseURL: "http://127.0.0.1:5173", trace: "on-first-retry", video: "on-first-retry", locale: "lo-LA" }, projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }, { name: "mobile-chrome", use: { ...devices["Pixel 7"] } }], webServer: { command: "corepack pnpm --filter @mekha/web dev -- --host 127.0.0.1", url: "http://127.0.0.1:5173", reuseExistingServer: !process.env.CI } });
